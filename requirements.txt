# ── Authentication ────────────────────────────────────────────────────────
bcrypt>=4.0.0
authlib>=1.3.0
requests>=2.31.0

# ── Cloud database (Supabase / PostgreSQL + pgvector) ─────────────────────
psycopg2-binary>=2.9.0

# ── LLM providers ─────────────────────────────────────────────────────────
groq>=0.9.0           # Free, fast — recommended for cloud
anthropic>=0.30.0     # Optional: Claude
openai>=1.40.0        # Optional: GPT-4 / OpenAI-compatible

# ── Local vector store (used in local mode, not cloud) ────────────────────
chromadb>=0.5.0

# ── Local embeddings — ONNX-based, low memory (~150MB vs 400MB for torch) ──
fastembed>=0.3.6

# ── Markdown parsing ──────────────────────────────────────────────────────
python-frontmatter>=1.0.0,<1.2.0
markdown>=3.6

# ── Config & env ──────────────────────────────────────────────────────────
PyYAML>=6.0
python-dotenv>=1.0.0

# ── CLI interface ─────────────────────────────────────────────────────────
rich>=13.7.0
prompt_toolkit>=3.0.0

# ── File-system watcher (live re-index for local vault) ───────────────────
watchdog>=4.0.0

# ── Web interface ─────────────────────────────────────────────────────────
flask>=2.3.0
flask-cors>=4.0.0
gunicorn>=21.0.0      # Production WSGI server (Render.com)

# ── Utilities ─────────────────────────────────────────────────────────────
tiktoken>=0.7.0
